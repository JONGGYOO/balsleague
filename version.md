# 발스리그 개발 히스토리

## v1.0 — 로그인 / 회원가입

- Clerk 기반 로그인·회원가입 페이지 구현 (`/sign-in`, `/sign-up`)
- Convex DB에 로그인 정보 및 개인정보 저장
- 회원가입 후 프로필 수정 페이지에서 개인정보(클랜소속·이름·닉네임) 저장
- 로그인 후 `/leagues`로 자동 이동

---

## v1.1 — 리그 목록

- 메인화면에 진행 중인 리그 전체 리스트 표시
- 리그 등록 기능: 년도·월·리그명 입력
- 관리자 전용 리그 수정·삭제 (소프트 삭제, 기록 보존)

---

## v1.2 — 스코어 입력

- 리그 참가 버튼 (참가 / 취소)
- 참가자를 셀렉트박스로 표시, 첫 글자 입력 시 실시간 필터링
- 나와 상대방의 스코어 입력 기능

---

## v1.3 — 순위표

- 입력된 스코어 기반 순위표 (승·무·패·득실 표시)
- 정렬 기준: 승점 → 득실 → 경기수 순

---

## v1.4 — 권한 설정

- 슈퍼관리자: `cotmul82@gmail.com` 고정
- 슈퍼관리자가 일반 사용자를 관리자로 지정 가능
- 슈퍼관리자·관리자: 리그 추가·수정·삭제·복원 가능
- 일반 사용자: 리그 참가 신청 후 관리자 승인 필요
- 삭제된 리그 데이터 보존, 슈퍼관리자만 복원 가능

---

## v1.5 — 경기 결과 리스트

- 순위표 클릭 시 경기 결과 리스트 페이지로 이동
- 최근 경기 결과 표시
- 순위표 선수 클릭 시 전체 리그 승·무·패 및 상대 전적 표시
- 경기 결과 최근순 정렬

---

## v1.6 — 메인화면 텍스트 한국어 변경

- "Sign in to blasleague" → 발스 리그
- "Welcome back! Please sign in to continue" → 발스 리그에 오신걸 환영합니다 회원가입 및 로그인 해주세요.
- "Don't have an account? Sign up" → 회원가입

---

## v2.0 — 버그 수정 1차

- 슈퍼유저 참가 승인대기 명단 미표시 수정
- 승인 대기 목록에 이름 없음으로 표시되는 오류 수정
- 로그인 후 새로고침 없이 리그 목록이 안 뜨는 오류 수정
- 스코어 0 입력 시 입력 버튼 비활성화 오류 수정
- 프로필 저장 후 이전 페이지로 이동

---

## v2.1 — 추가 기능 1차

- 참가자 인원수 클릭 시 참가자 전체 리스트 팝업
- 순위표 전체 경기 보기 버튼 제거
- 최근 경기 결과 5건만 표시, 전체보기 버튼 추가

---

## v2.2 — 추가 기능 2차

- 선수 클릭 시: 현재 리그 통계 → 전체 통계 → 상대 전적(현재/전체 리그) 순 표시
- 로그인 사용자 기준 상대 전적만 표시
- 최근 경기 결과 전체보기 시 전체 목록, 기본은 5건만 표시

---

## v2.3 — 추가 기능 3차

- 리그 추가 시 리그명 기본값 "발스리그" 자동 입력
- 최초 로그인 후 프로필 1회 저장 필수, 미저장 시 안내 메시지 표시
- 내 프로필: 클랜소속 셀렉트박스(관리자 입력값), 이름·닉네임·생년월일·핸드폰번호 필드 추가
- 생년월일 셀렉트박스, 핸드폰번호 010 자동 설정 + 4자리씩 2개 입력

---

## v2.4 — 추가 기능 4차

- 프로필 모든 항목 필수 입력 처리
- 내 프로필에 홈으로 가는 버튼 추가
- 우측 상단에 닉네임(이름) 표시

---

## v2.5 — 추가 기능 5차 및 버그 수정 2차

- 동일 상대와 스코어 재입력 시 "이미 경기를 했습니다. 추가로 입력하겠습니까?" 확인 메시지
- 이메일 로그인 후 새로고침 없이 리그 목록 표시 안 되는 오류 수정
- 리그 목록 연도별 셀렉트박스 필터 (리그 있는 연도만 표시, 선택 즉시 조회)
- 선수 클릭 통계 순서 변경: 현재 리그 통계 → 상대 전적(현재 리그) → 경기 기록(현재 리그) → 전체 통계 → 상대 전적(전체) → 경기 기록(전체)
- 경기 기록(현재 리그) 항목 신규 추가

---

## v2.6 — 버그 수정 3차

- 슈퍼관리자·관리자도 리그 참가 신청 가능하도록 수정
- 리그 목록 연도 셀렉트박스 전체 조회 오류 수정
- 연도가 1개뿐이어도 셀렉트박스 표시
- 리그 목록 로딩 속도 튜닝 (단일 쿼리로 통합)
- 경기 기록 전체리그: 5경기만 표시, 클릭 시 전체 목록 페이지 이동 및 연도별 조회 조건 추가

---

## v3.0 — 배포 준비

- Clerk v7 로그인 redirect prop 오류 수정 (`afterSignInUrl` → `signInFallbackRedirectUrl`)
- 아이디 로그인 시 렌더링 지연 오류 수정
- Convex 프로덕션 배포 (`necessary-beagle-620`)
- GitHub 저장소 생성 및 푸시 (`JONGGYOO/balsleague`)
- Vercel 배포 연동

---

## v3.1 — 추가 기능 6차

- 리그 참가중 취소 기능
- 사용자 역할 탭 관리자 공개 및 상세 정보 표시

---

## v4.0 — 내전 기능 도입

- 내전(innerwar) 기능 전체 구현

---

## v4.1 — 추가 기능 7차

- 점수 입력 권한 확장: 경기 양쪽 선수도 점수 입력 가능
- 내기 전달 화면 개발 및 상단 안내 문구 정리

---

## v4.2 — 버그 수정 4차

- 경기 결과 삭제 기능 추가

---

## v4.3 — 내전 기능 개선 및 공유 미리보기

- 내전 순번 변경 권한 확장
- 링크 공유 시 OG 이미지 및 내기품목 표시
- 미출전 인원 제외 기능, 내기품목 위치 조정
- 루트 경로(`/`) 인증 보호 제외, 정적 이미지 파일이 인증 미들웨어에 막혀 OG 미리보기 안 뜨던 문제 수정

---

## v5.0 — 성적기반 배정 기능

- 성적기반 배정 기능 신규 도입: 리그 성적에 내전 전적을 반영해 배정 순위·점수 산출
- 배정 가중치 리그:내전 = 8:2로 조정 (최초 7:3에서 변경)
- 경기 기록이 전혀 없는 참가자는 최하위로 배치
- 배정 결과 화면에 순위·배점 표시
- 가중치 문구 오타 및 오해 소지 있는 표시 수정
- 미기록 종목을 0.5로 가정해 순위가 뒤바뀌던 버그, 삭제된 내전 기록이 점수 계산에 섞이던 버그 수정
- `Grade.md`를 현재 구현 기준으로 재정리

---

## v5.1 — 버그 수정 5차

- 성적기반 배정: 합산 경기 수가 3경기 미만인 참가자는 "전적없음"과 동일하게 항상 최하위로 배치
  (예: 1경기 0승이 스무딩으로 40%가 되어, 훨씬 많이 뛰고도 실력이 검증된 약체보다 순위가 앞서던 역전 현상 수정)

---

## v6.0 — 추가 기능 8차

- 리그 순위표: 승·무·패·득실 기반 승점(승 3점·무 1점·패 0점) 계산 및 승점 기준 순위 정렬, 승점 컬럼 표시
- 내전: 점수 저장 후 다음 경기로 넘어간 직후 바로 이전(가장 최근 확정) 경기의 점수를 다시 수정할 수 있는 기능 추가
  (다음 경기에 점수가 입력되면 잠김, 우승팀이 뒤바뀌는 경우 이후 진행 상태 자동 재계산)
- 리그 종료 버튼 추가 (슈퍼관리자·관리자 전용, 리그 상세 페이지): 종료 시 순위 확정 및 1위 참가자에게 우승 기록 부여,
  종료 후 일반 사용자는 입력/수정 불가하나 관리자는 계속 수정 가능
- 리그 우승 횟수에 따라 닉네임이 표시되는 모든 화면(순위표·참가자 목록·경기 결과·내전 팀 구성·관리자 패널 등)에
  별/왕관 배지 표시: 1~4회 별, 5~9회 왕관+별, 10회 이상 화려한 왕관
- 선수 상세 페이지에 내전 통계 섹션 추가: 내전 전체 승·무·패·득실, 상대전적, 최근 5경기 및 전체보기(신규 페이지)
- 내전 상세 페이지에서 선수 닉네임 클릭 시 선수 상세 페이지로 이동하도록 링크 추가

---

## v6.1 — 버그 수정 6차

- 리그 선수 클릭 시 표시되던 페이지(`/players/[userId]`)에 내전 통계가 섞여 나오던 것을 리그 전용 화면으로 원복
- 내전 선수 클릭 시 보여줄 화면을 `/players/[userId]/innerwar`로 완전히 분리 신규 생성
  (현재 내전 통계 → 상대전적 현재내전 → 경기 기록 현재 내전 → 전체 통계(모든 내전) → 상대전적 전체내전 →
  경기기록 전체내전 순, 리그 화면과 동일한 구성으로 제작)
- 내전 상세 페이지의 선수 닉네임 링크를 새 내전 전용 화면으로 연결하도록 수정
- 리그 종료 버튼을 헤더에서 페이지 본문("리그 관리" 카드)으로 이동

---

## v6.2 — 추가 기능 9차

- 리그·내전 목록 상단의 "발스리그" 타이틀 제거, 상단 메뉴(리그·내전)를 좌측 정렬로 정리
- 상단 메뉴에 Award 탭 추가, `/awards` 페이지 신규 생성
- 월별 우수 클랜원 시상: 리그 경기 1경기당 2점, 내전 경기 1경기당 1점(승패 무관, 참여 기준)으로 실시간 집계해
  전체 사용자 순위 표시, 2026년 7월부터 월 선택 가능, 각 행에 산출근거(경기 수 × 배점) 표시

---

## v6.3 — 추가 기능 10차

- 내전 경기 시작 전 팀 순번 고정/해제 기능 추가: 본인(또는 관리자)이 자신의 순번을 고정하면
  다른 인원이 그 자리로 스왑해 들어올 수 없음
- 순번 이동 시 고정된 자리는 건너뛰고 다음으로 이동 가능한 자리와 스왑
  (예: 2번이 고정된 상태에서 3번이 위로 이동하면 1번으로 이동, 2번은 그대로 유지 —
  고정된 자리가 연속으로 여러 개여도 그만큼 건너뜀)
- 고정된 자리는 순번 이동 버튼 대신 잠금 아이콘으로 표시

---

## v6.4 — 버그 수정 7차

- Award(월별 우수 클랜원 시상) 집계 기준 수정: 리그/내전이 속한 연·월이 아니라
  경기가 실제로 기록된(입력된) 시각 기준으로 그 달 집계에 포함되도록 변경
  (예: "2026년 6월" 리그가 계속 진행 중이어도 실제 경기를 7월에 치렀다면 7월 집계에 포함)
- 서버 실행 환경 시간대와 무관하게 한국시간(KST) 기준으로 날짜를 판정하도록 수정
- 화면의 산출 근거 안내 문구도 새 집계 기준에 맞게 수정

---

## v6.5 — 버그 수정 8차

- 내전 경기가 종료(done)된 후에는 "팀 현황" 섹션이 사라지면서, 남은 "전체 경기 결과"·
  "개인 통계"·"내기 전달" 영역의 선수 닉네임에 선수 상세 페이지 링크가 없어 클릭이 안 되던 문제 수정.
  경기 종료 후에도 위 세 영역 모두 선수 닉네임 클릭 시 내전 통계 페이지로 이동 가능

---

## v6.6 — 버그 수정 9차

- 내전 경기 진행 중 "✕"로 제외한 선수의 빈자리에, 이후 새로 참가 신청한 선수가 들어갈 수 없던 문제 수정
  (경기 시작 후에는 팀 배정이 잠겨 있어 신규 참가자가 팀 미배정 상태로 남아있었음)
- 경기 진행 중(inProgress) 참가 신청 시 A팀/B팀을 선택할 수 있도록 변경, 선택한 팀의 맨 뒤 순번으로 즉시
  배정되어 빈자리를 채울 수 있음 — 내전 목록·상세 페이지 모두 적용

---

## v6.7 — 기능 추가 1차

- 리그 참가자를 리그 페이지에서 클릭했을 때 이동하는 선수 상세화면에 "아직 대결 안 한 참가자" 목록 추가
  (해당 선수 기준으로 같은 리그의 참가자 중 아직 맞대결 기록이 없는 사람만 표시)
- 리그 페이지의 "참가자 N명 ▼" 화살표 토글과 동일한 방식으로 펼치고 접을 수 있음

---

## v6.8 — 기능 추가 2차

- 내전 순번 고정/해제 버튼이 경기 시작(inProgress) 후에는 사라지던 문제 수정 — 경기가 종료(done)되기
  전까지는 계속 노출되어 본인은 자신의 순번을, 슈퍼관리자·관리자는 다른 인원의 순번도 고정/해제 가능
- "팀 현황"(경기 진행 중) 영역에도 순번 고정/해제 버튼을 추가로 노출
