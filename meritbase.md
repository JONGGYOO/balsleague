# 성적기반 배정 로직 정리

`assignTeamsByScore` (convex/innerwars.ts:272-327) — "성적기반 배정" 버튼 클릭 시 실행되는 뮤테이션.

## 1. 대상자 필터링

- 해당 내전(`innerwarId`)의 참가자를 최대 200명까지 조회 (`innerwarParticipants`, `by_innerwar` 인덱스).
- `status`가 없거나 `"approved"`인 참가자만 "승인된 참가자(approved)"로 간주.
- 승인된 참가자가 2명 미만이면 에러 발생 ("최소 2명 이상이 필요합니다.").

## 2. 승수(wins) 집계 — 배정 기준 점수

- `scores` 테이블에서 **최대 2000건**을 조회. ⚠️ **리그(leagueId)로 필터링하지 않음** — 특정 리그에 국한되지 않고 시스템 전체 경기 기록(`scores` 컬렉션 전체)을 대상으로 함.
- 각 승인된 참가자(`p`)에 대해, 아래 조건을 만족하는 `scores` 레코드 개수를 승수로 계산:
  - `homeUserId === p.userId` 이고 `homeScore > awayScore` (홈으로 이겼을 때), 또는
  - `awayUserId === p.userId` 이고 `awayScore > homeScore` (어웨이로 이겼을 때)
- 무승부(`homeScore === awayScore`)는 승리로 카운트되지 않음. 패배도 카운트되지 않음(승수만 집계, 패수/득실차 등은 반영 안 함).
- 즉, **배정 기준은 "전체 경기 기록상 승리 횟수(총 승수)"** 하나뿐이며, 승률·최근 성적·최근 폼·팀 내전 성적 등은 전혀 반영하지 않음.

## 3. 팀 배정 알고리즘 (스네이크 드래프트 아님, 단순 지그재그)

1. 승인된 참가자를 **승수 내림차순**으로 정렬 (`winCounts` 기준, 동점자의 정렬 순서는 안정 정렬 - 원래 조회 순서 유지).
2. 정렬된 순서대로 순회하며 인덱스 `i`의 짝/홀에 따라 팀 배정:
   - `i % 2 === 0` → A팀
   - `i % 2 === 1` → B팀
   - 즉 1등 A팀, 2등 B팀, 3등 A팀, 4등 B팀 ... 순서로 번갈아 배정 (지그재그 방식).
3. 각 팀 내 순서(`teamOrder`)는 배정된 순서대로 0부터 증가하며 부여됨.

### 참고: 승수가 같을 때 우선순위
- 승수가 동일한 참가자들 사이의 순서는 `Array.prototype.sort`가 안정 정렬이므로 원래 배열(`approved`, 즉 `innerwarParticipants`에서 조회된 순서 = 참가 등록/저장 순서)의 순서가 그대로 유지됨. 별도의 2차 기준(참가 등록일, 이름 등)은 없음.

## 4. 배정 이후 처리

- 기존에 등록된 해당 내전의 `innerwarMatches`(최대 500건 조회)를 모두 삭제 (재배정 시 이전 경기 조합 초기화).
- 내전 상태(`status`)를 `"teamAssigned"`로 변경.
- `currentIndexA`, `currentIndexB`를 0으로 초기화 (경기 진행 순번 포인터 리셋).
- 기존 `winnerTeam`, `currentIndexA`, `currentIndexB` 필드는 제거 후 재설정(`ctx.db.replace` 사용).

## 요약 (한 줄)

**전체 경기 기록(scores, 리그 무관, 최대 2000건) 기준 "승리 횟수"가 많은 순서대로 정렬한 뒤, 1-2-1-2 지그재그(짝수 인덱스=A팀, 홀수 인덱스=B팀) 방식으로 A/B팀에 번갈아 배정한다.** 승수 외 다른 지표(승률, 득실차, 최근 폼)는 고려하지 않으며, 동점자는 참가자 목록 조회 순서를 그대로 따른다.
